<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.careyq.explore.server.mapper.CategoryMapper">

    <select id="selectCategories" resultType="com.careyq.explore.server.vo.CategoryVO">
        select c.id,
               c.name,
               c.alias,
               c.hits,
               c.sort,
               ifnull(a.count, 0) as count
        from postCategoryTag c
                 left join (select category_id, count(*) count from blog_post
                            where is_del = 0 group by category_id) a on c.id = a.category_id
        where c.is_del = 0 order by sort
    </select>
</mapper>
